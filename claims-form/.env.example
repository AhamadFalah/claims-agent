# Copy to `.env` and set the URL of your n8n webhook (Production or Test).
# This is the ONLY backend the form talks to. The Attio API token lives
# inside n8n — never put it here (anything VITE_* is bundled into the browser).
VITE_N8N_WEBHOOK_URL=https://your-n8n-host/webhook/claims-intake
